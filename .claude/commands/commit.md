---
description: Commit only the files you authored this session
argument-hint: [optional hint about commit message focus]
---

Commit only the files you (Claude) directly Edited or Wrote in this conversation. Leave everything else alone — pre-existing modifications, files the user changed, files from other agents, untracked files you didn't create.

## Steps

1. Run `git status` and `git diff` in parallel to see all working-tree changes.
2. Build a list of files you touched in this session by scanning your own prior `Edit` and `Write` tool calls. That exact set is what you stage. New files created via `Write` belong on the list; files modified by anyone else stay off it.
3. Stage that list explicitly with `git add <file1> <file2> ...` — always name each file.
4. If dirty or untracked files exist outside your set, list them in your response under "**Not staged (not authored this session):**" so the user knows they were skipped on purpose.
5. If your set is empty, say so and stop — leave the working tree alone.
6. Match this repo's existing commit style: run `git log --oneline -10` and follow whatever convention you see (e.g. Conventional Commits `type(scope): description`, lowercase, imperative, no trailing period). Honor any commit guidance in `CLAUDE.md`.
7. Write a message focused on **why**, not what — the diff is the what. One subject line + a short body if non-obvious.
8. End the message at the body. Skip any `Co-Authored-By` or "generated by Claude" trailer. Pass the message via HEREDOC.
9. Stop after `git commit`. Pushing is the user's call. Use plain `git commit -m` — leave `--no-verify`, `--amend`, and other rewrite flags out.
10. After committing, run `git status` and confirm the commit hash + landed files in your response.

## Arguments

If the user passed `$ARGUMENTS`, treat it as a hint to steer the commit message (e.g. emphasis, scope choice, or framing). It is guidance, not a literal message.
