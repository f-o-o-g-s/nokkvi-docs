{
  "albums_artwork_overlay": {
    "description": "Show a text overlay on artwork thumbnails in the Albums view."
  },
  "albums_ascending": {
    "description": "Ascending order in the Albums view."
  },
  "albums_show_index": {
    "description": "Show the row index column in the albums view."
  },
  "albums_show_love": {
    "description": "Show the loved/heart column in the albums view."
  },
  "albums_show_plays": {
    "description": "Show the play count column in the albums view."
  },
  "albums_show_select": {
    "description": "Show the multi-select checkbox column in the albums view."
  },
  "albums_show_songcount": {
    "description": "Show the song count column in the albums view."
  },
  "albums_show_stars": {
    "description": "Show the star rating column in the albums view."
  },
  "albums_show_thumbnail": {
    "description": "Show the thumbnail column in the albums view."
  },
  "albums_sort": {
    "description": "Sort mode for the Albums view."
  },
  "artists_artwork_overlay": {
    "description": "Show a text overlay on artwork thumbnails in the Artists view."
  },
  "artists_ascending": {
    "description": "Ascending order in the Artists view."
  },
  "artists_show_albumcount": {
    "description": "Show the album count column in the artists view."
  },
  "artists_show_index": {
    "description": "Show the row index column in the artists view."
  },
  "artists_show_love": {
    "description": "Show the loved/heart column in the artists view."
  },
  "artists_show_plays": {
    "description": "Show the play count column in the artists view."
  },
  "artists_show_select": {
    "description": "Show the multi-select checkbox column in the artists view."
  },
  "artists_show_songcount": {
    "description": "Show the song count column in the artists view."
  },
  "artists_show_stars": {
    "description": "Show the star rating column in the artists view."
  },
  "artists_show_thumbnail": {
    "description": "Show the thumbnail column in the artists view."
  },
  "artists_sort": {
    "description": "Sort mode for the Artists view."
  },
  "artwork_auto_max_pct": {
    "description": "Max artwork size in `auto` mode, as a fraction of the window's short axis (0.30–0.70)."
  },
  "artwork_column_mode": {
    "description": "Visibility and sizing of the large artwork panel shown alongside list views. Options: `auto` (size derived from window width; auto-hides when the slot list would have less than 800px), `always_native` (user-defined width; image stays square and letterboxed vertically), `always_stretched` (user-defined width; image fills the column non-square per `artwork_column_stretch_fit`), `always_vertical_native` (user-defined height; artwork stacked above the slot list, stays square), `always_vertical_stretched` (user-defined height; artwork stacked above the slot list, fills the rect via `artwork_column_stretch_fit`), `never` (column hidden everywhere)."
  },
  "artwork_column_stretch_fit": {
    "description": "Fit mode used when `artwork_column_mode = \"always_stretched\"`. Options: `cover` (preserve aspect ratio, crop to fill), `fill` (true stretch — distorts album art). Ignored in other modes."
  },
  "artwork_column_width_pct": {
    "description": "Artwork column width as a fraction of the window width (0.05 to 0.80). Only consulted in `always_native` and `always_stretched` modes."
  },
  "artwork_resolution": {
    "description": "Quality for large panel artwork. Options: `default` (1000px), `high` (1500px), `ultra` (2000px), `original`."
  },
  "artwork_vertical_height_pct": {
    "description": "Artwork height as a fraction of window height (0.10–0.80). Only consulted in `always_vertical_native` and `always_vertical_stretched` modes."
  },
  "auto_follow_playing": {
    "description": "The Queue view auto-scrolls to keep the currently playing track visible as it changes."
  },
  "autohide_collapsed_appearance": {
    "description": "What the auto-hide toolbar shows while collapsed. Options: `hairline` (a thin sliver with an optional accent grip — hover to reveal), `hidden` (nothing; the list reclaims the space, but flicking the mouse to the top edge or using a sort/search hotkey still reveals it), `count_strip` (a slim strip echoing the current sort and item count, without controls). Only applies when `autohide_toolbar` is on."
  },
  "autohide_toolbar": {
    "description": "Auto-hide the view-header toolbar (sort and search controls) to a thin line until you hover it or use a sort/search shortcut."
  },
  "autohide_toolbar_grip": {
    "description": "Show a centered accent grip bar on the collapsed `hairline` sliver. Only applies when `autohide_toolbar` is on and `autohide_collapsed_appearance = \"hairline\"`."
  },
  "autohide_toolbar_height": {
    "description": "Height in pixels of the collapsed auto-hide toolbar. Only applies when `autohide_toolbar` is on."
  },
  "bit_perfect": {
    "description": "Bit-perfect output mode for the Linux/PipeWire backend. Options: `off` (standard 48 kHz DSP path — EQ, software volume, and crossfade all apply), `strict` (untouched samples to the DAC at each track's native rate, hard-cut between every track), `relaxed` (untouched bodies, but crossfades adjacent tracks that share a sample rate and channel count using `crossfade_duration_secs` — only that few-second blend isn't bit-perfect). Strict and Relaxed bypass the EQ, software volume, limiter, and ReplayGain/AGC (volume moves to the PipeWire node) and switch off `crossfade_enabled`. Needs PipeWire rate-switching (`allowed-rates`) configured. See the [Audio guide](/guides/audio/#bit-perfect-output)."
  },
  "close_to_tray": {
    "description": "When `show_tray_icon` is on, the window's close button hides Nokkvi into the tray instead of quitting. Ignored when the tray icon is disabled."
  },
  "crossfade_duration_secs": {
    "description": "Duration of the crossfade in seconds (1 to 12)."
  },
  "crossfade_enabled": {
    "description": "Enable smooth transitions between tracks. Mutually exclusive with `bit_perfect` (Strict/Relaxed)."
  },
  "custom_eq_presets": {
    "description": "User-saved EQ presets. Each preset is a `{ name, gains }` object; managed through the in-app EQ panel."
  },
  "enter_behavior": {
    "description": "Action when pressing `Enter` on a song. Options: `play_all` (replace queue with the current view), `play_single` (replace queue with just this track), `append_and_play` (add to queue and play)."
  },
  "enter_shuffle": {
    "description": "When enabled, pressing `Enter` (or clicking) to play a collection starts it in a fresh random order instead of list order. `Ctrl + Enter` forces a one-shot shuffle regardless of this setting."
  },
  "eq_enabled": {
    "description": "Global bypass for the 10-band equalizer."
  },
  "eq_gains": {
    "description": "List of 10 gain values in dB (one per band: 32, 64, 125, 250, 500, 1k, 2k, 4k, 8k, 16k Hz). Range −12.0 to +12.0 per band."
  },
  "font_family": {
    "description": "Font family override. Leave empty to use system default sans-serif."
  },
  "genres_ascending": {
    "description": "Ascending order in the Genres view."
  },
  "genres_show_albumcount": {
    "description": "Show the album count column in the genres view."
  },
  "genres_show_index": {
    "description": "Show the row index column in the genres view."
  },
  "genres_show_select": {
    "description": "Show the multi-select checkbox column in the genres view."
  },
  "genres_show_songcount": {
    "description": "Show the song count column in the genres view."
  },
  "genres_show_thumbnail": {
    "description": "Show the thumbnail column in the genres view."
  },
  "genres_sort": {
    "description": "Sort mode for the Genres view."
  },
  "horizontal_volume": {
    "description": "Stack volume sliders horizontally instead of vertically in the player bar."
  },
  "icon_set": {
    "description": "Which icon family the UI renders its glyphs from. Options: `phosphor` (default — Phosphor glyphs at Regular weight, with the transport and rating glyphs forced to Fill so the play button and stars stay solid), `lucide` (the original thin stroked outlines the app first shipped)."
  },
  "library_page_size": {
    "description": "How many items to fetch per request. Options: `small` (100), `default` (500), `large` (1000), `massive` (5000)."
  },
  "light_mode": {
    "description": "Use the light variant of the active theme."
  },
  "local_music_path": {
    "description": "Absolute path to your local music library. If set, Nokkvi will try to open files directly (for things like \"Show in File Manager\")."
  },
  "mini_player_show_modes": {
    "description": "Show the playback-mode toggles (repeat, shuffle, consume, EQ, sound effects, crossfade, bit-perfect, visualizer) and their kebab (⋮) overflow in the mini-player bar. Only applies when `track_info_display = \"mini_player\"`."
  },
  "mini_player_show_volume": {
    "description": "Show the volume control in the mini-player bar. Only applies when `track_info_display = \"mini_player\"`."
  },
  "nav_display_mode": {
    "description": "Content of tabs. Options: `text_only`, `text_and_icons`, `icons_only`."
  },
  "nav_layout": {
    "description": "Placement of view tabs. Options: `top` (horizontal bar), `side` (vertical sidebar), `none` (hidden — only the active view and player bar render)."
  },
  "normalization_level": {
    "description": "Target loudness. Options: `quiet` (-23 LUFS), `normal` (-14 LUFS), `loud` (-9 LUFS)."
  },
  "opacity_gradient": {
    "description": "Apply a subtle opacity fade to items that are not in the center of the list."
  },
  "playlists_artwork_overlay": {
    "description": "Show a text overlay on artwork thumbnails in the Playlists view."
  },
  "playlists_ascending": {
    "description": "Ascending order in the Playlists view."
  },
  "playlists_show_duration": {
    "description": "Show the total duration column in the playlists view."
  },
  "playlists_show_index": {
    "description": "Show the row index column in the playlists view."
  },
  "playlists_show_select": {
    "description": "Show the multi-select checkbox column in the playlists view."
  },
  "playlists_show_songcount": {
    "description": "Show the song count column in the playlists view."
  },
  "playlists_show_thumbnail": {
    "description": "Show the thumbnail column in the playlists view."
  },
  "playlists_show_updatedat": {
    "description": "Show the last updated column in the playlists view."
  },
  "playlists_sort": {
    "description": "Sort mode for the Playlists view."
  },
  "queue_ascending": {
    "description": "Ascending order in the Queue view."
  },
  "queue_show_album": {
    "description": "Show the album column in the queue."
  },
  "queue_show_default_playlist": {
    "description": "Show a chip in the Queue view's header for the remembered default playlist (the one used by `quick_add_to_playlist`)."
  },
  "queue_show_duration": {
    "description": "Show the duration column in the queue."
  },
  "queue_show_genre": {
    "description": "Show the genre column in the queue."
  },
  "queue_show_index": {
    "description": "Show the row index column in the queue."
  },
  "queue_show_love": {
    "description": "Show the loved/heart column in the queue."
  },
  "queue_show_plays": {
    "description": "Show the play count column in the queue."
  },
  "queue_show_select": {
    "description": "Show the multi-select checkbox column in the queue."
  },
  "queue_show_stars": {
    "description": "Show the star rating column in the queue."
  },
  "queue_show_thumbnail": {
    "description": "Show the thumbnail column in the queue."
  },
  "queue_sort": {
    "description": "Sort mode for the Queue view (last applied; manual reorder works from any mode)."
  },
  "quick_add_to_playlist": {
    "description": "Skip the \"Add to Playlist\" dialog and add directly to a remembered default playlist."
  },
  "rating_change_notification_enabled": {
    "description": "Show a desktop notification confirming the new rating whenever you rate via the rating hotkey or the `nokkvi rate` command — handy when the window is minimized or on another workspace. Only fires on a real change, so a no-op like `+1` at 5 stars stays silent."
  },
  "rating_reminder_enabled": {
    "description": "Show a \"rate this track\" desktop notification after you finish listening to a song. When off, no reminder is shown."
  },
  "rating_reminder_percent": {
    "description": "Percentage of the track that must play before the reminder fires when `rating_reminder_trigger = \"percentage_played\"`. Range 60–90. Ignored by the `on_scrobble` trigger."
  },
  "rating_reminder_trigger": {
    "description": "When the reminder fires. Options: `on_scrobble` (when the server confirms the play — requires scrobbling to be active), `percentage_played` (once `rating_reminder_percent` of the track has elapsed)."
  },
  "replay_gain_fallback_db": {
    "description": "dB applied to tracks with no ReplayGain tags. Range -15 to +15. Ignored when `replay_gain_fallback_to_agc` is `true`."
  },
  "replay_gain_fallback_to_agc": {
    "description": "Untagged tracks fall through to real-time AGC instead of using `replay_gain_fallback_db`."
  },
  "replay_gain_preamp_db": {
    "description": "Pre-amp dB applied on top of the resolved ReplayGain value. Range -15 to +15. Typical: `0` (reference), `+6` (modern-loudness target)."
  },
  "replay_gain_prevent_clipping": {
    "description": "Clamp the resolved gain so `track_peak × gain ≤ 1.0` using the per-track peak tag."
  },
  "rewind_on_previous": {
    "description": "When enabled, pressing Previous restarts the current track instead of stepping to the previous one, once it has played past 5 seconds."
  },
  "rounded_mode": {
    "description": "Rounded corners on UI elements. Options: `off` (flat throughout), `on` (rounded everywhere), `player_only` (rounded player bar/progress/volume/transport and bottom metadata strip; rest of the UI stays flat)."
  },
  "scrobble_threshold": {
    "description": "Portion of the track you must actually listen to before a scrobble fires (0.25 to 0.90). A scrobble also fires after 4 minutes of listening regardless of this value — whichever comes first."
  },
  "scrobbling_enabled": {
    "description": "Report listening activity back to the server."
  },
  "scrollbar_visibility": {
    "description": "How the slot-list scrollbar is shown. Options: `on_hover` (transient fade handle, reserves no width), `always` (permanent track, reserves a right-side gutter), `hidden` (no scrollbar; mouse-wheel still scrolls)."
  },
  "server_url": {
    "description": "The full URL of your Navidrome server (e.g., `https://music.example.com`)."
  },
  "sfx_volume": {
    "description": "Volume for UI sound effects (0.0 to 1.0)."
  },
  "show_album_artists_only": {
    "description": "If true, the Artists view shows only album artists and excludes track-level featured artists."
  },
  "show_tray_icon": {
    "description": "Register a StatusNotifierItem tray icon on the session bus. When off, no tray icon is shown."
  },
  "similar_show_album": {
    "description": "Show the album column in the similar songs view."
  },
  "similar_show_duration": {
    "description": "Show the duration column in the similar songs view."
  },
  "similar_show_index": {
    "description": "Show the row index column in the similar songs view (Find Similar / Top Songs results)."
  },
  "similar_show_love": {
    "description": "Show the loved/heart column in the similar songs view."
  },
  "similar_show_select": {
    "description": "Show the multi-select checkbox column in the similar songs view."
  },
  "similar_show_thumbnail": {
    "description": "Show the thumbnail column in the similar songs view."
  },
  "slot_row_height": {
    "description": "Row density in lists. Options: `compact` (50px), `default` (70px), `comfortable` (90px), `spacious` (110px)."
  },
  "slot_text_links": {
    "description": "Make artist and album names inside list rows clickable links to their respective views."
  },
  "songs_artwork_overlay": {
    "description": "Show a text overlay on artwork thumbnails in the Songs view."
  },
  "songs_ascending": {
    "description": "Ascending order in the Songs view."
  },
  "songs_show_album": {
    "description": "Show the album column in the songs view."
  },
  "songs_show_duration": {
    "description": "Show the duration column in the songs view."
  },
  "songs_show_genre": {
    "description": "Show the genre column in the songs view."
  },
  "songs_show_index": {
    "description": "Show the row index column in the songs view."
  },
  "songs_show_love": {
    "description": "Show the loved/heart column in the songs view."
  },
  "songs_show_plays": {
    "description": "Show the play count column in the songs view."
  },
  "songs_show_select": {
    "description": "Show the multi-select checkbox column in the songs view."
  },
  "songs_show_stars": {
    "description": "Show the star rating column in the songs view."
  },
  "songs_show_thumbnail": {
    "description": "Show the thumbnail column in the songs view."
  },
  "songs_sort": {
    "description": "Sort mode for the Songs view."
  },
  "sound_effects_enabled": {
    "description": "Global toggle for UI sound effects."
  },
  "stable_viewport": {
    "description": "Clicking an item highlights it in place; the list does not auto-scroll on playback changes."
  },
  "start_view": {
    "description": "The view to show on startup. Options: `Queue`, `Albums`, `Artists`, `Songs`, `Genres`, `Playlists`, `Radios`."
  },
  "strip_click_action": {
    "description": "Action on click. Options: `go_to_queue`, `go_to_album`, `go_to_artist`, `copy_track_info`, `do_nothing`."
  },
  "strip_merged_mode": {
    "description": "Apply strip patterns to the merged artist/album display string rather than individual fields."
  },
  "strip_separator": {
    "description": "Character used to join visible fields in the metadata strip. Options: `dot` (·), `bullet` (•), `pipe` (|), `em_dash` (—), `slash` (/), `bar` (│)."
  },
  "strip_show_album": {
    "description": "Show the album name in the metadata strip."
  },
  "strip_show_artist": {
    "description": "Show the artist name in the metadata strip."
  },
  "strip_show_format_info": {
    "description": "Show technical info (codec/sample rate) in the strip."
  },
  "strip_show_labels": {
    "description": "Show the field labels (`title:` / `artist:` / `album:`) before each value in the metadata strip."
  },
  "strip_show_title": {
    "description": "Show the song title in the metadata strip."
  },
  "suppress_library_refresh_toasts": {
    "description": "Hide the toast notification that appears when Navidrome reports a library refresh."
  },
  "track_info_display": {
    "description": "Placement of the metadata strip. Options: `off`, `player_bar`, `top_bar`, `progress_track`."
  },
  "username": {
    "description": "Your Navidrome username."
  },
  "verbose_config": {
    "description": "How `config.toml` is rewritten when you change settings. Options: `off` (sparse — only non-default keys, with `# description` comments above each `[visualizer]` key), `on` (writes every setting, including unchanged defaults — a full template for manual editing), `clean` (sparse like `off`, but no comments). Legacy `true`/`false` values still load (`true` → `on`, `false` → `off`)."
  },
  "visualization_mode": {
    "description": "Active visualizer mode. Options: `off`, `bars`, `lines`, `scope`. Cycle live with the `V` hotkey — see [Visualizer Reference](/reference/visualizer/)."
  },
  "visualizer.auto_sensitivity": {
    "description": "Automatically scale gain to fill the available height."
  },
  "visualizer.bars.bar_depth_3d": {
    "description": "3D isometric depth in pixels (0.0 disables; up to 20.0)."
  },
  "visualizer.bars.bar_spacing": {
    "description": "Gap between bars in pixels."
  },
  "visualizer.bars.bar_width_max": {
    "description": "Maximum bar width in pixels for large windows. Must be ≥ `bar_width_min` (up to 20.0). Equal min/max gives uniform bar widths regardless of window size."
  },
  "visualizer.bars.bar_width_min": {
    "description": "Minimum bar width in pixels for small windows (1.0 to 10.0)."
  },
  "visualizer.bars.border_width": {
    "description": "Border thickness in pixels. Also controls LED segment gaps when `led_bars` is on (0.0 to 5.0)."
  },
  "visualizer.bars.echo": {
    "description": "Milkdrop-style zoom/rotate feedback — the bars spiral and tunnel into themselves, swirling with the bass/beat (0.0 disables; 1.0 = strong persistence). Takes over the display when on."
  },
  "visualizer.bars.flash_intensity": {
    "description": "Peak-flash bloom strength for Bars mode (0.0 disables; up to 1.0). Bars bloom toward the peak color on transients/beats."
  },
  "visualizer.bars.gradient_mode": {
    "description": "Color mapping mode. [Learn more &rarr;](/reference/visualizer/#gradient-modes)"
  },
  "visualizer.bars.gradient_orientation": {
    "description": "Axis for color mapping (`vertical` or `horizontal`)."
  },
  "visualizer.bars.led_bars": {
    "description": "Render bars as discrete LED segments (VU meter style)."
  },
  "visualizer.bars.led_segment_height": {
    "description": "Height of each LED segment in pixels (LED mode only; 2.0 to 20.0)."
  },
  "visualizer.bars.max_bars": {
    "description": "Maximum number of bars the dynamic layout will try to fit (16 to 2048)."
  },
  "visualizer.bars.peak_fade_time": {
    "description": "Fade-out duration in milliseconds (`fade` and `fall_fade` modes only)."
  },
  "visualizer.bars.peak_fall_speed": {
    "description": "Peak fall velocity scale (1 = slow, 5 = default, 20 = fast; does not affect `fade` mode)."
  },
  "visualizer.bars.peak_gradient_mode": {
    "description": "Color mode for peaks (`static`, `cycle`, `height`, `match`)."
  },
  "visualizer.bars.peak_height_ratio": {
    "description": "Peak indicator height as a percentage of bar width (10 to 100; ignored in LED mode)."
  },
  "visualizer.bars.peak_hold_time": {
    "description": "Duration in milliseconds peaks stay at maximum before falling/fading."
  },
  "visualizer.bars.peak_mode": {
    "description": "Behavior of peak indicators. [Learn more &rarr;](/reference/visualizer/#peak-behavior)"
  },
  "visualizer.bars.placement": {
    "description": "Where the Bars visualizer is drawn. Options: `over_cover` (over the now-playing cover art on the Queue view, while playing), `bottom_band` (a band across the bottom of the window, above the player bar, on every view)."
  },
  "visualizer.bars.trails": {
    "description": "Motion trails — bars leave a fading after-image (0.0 disables; 1.0 = long comet trails)."
  },
  "visualizer.beat_reactivity": {
    "description": "How strongly effects pump on the beat and bass drops (0.0 = static/loudness-only, 1.0 = full punch). Scales the bloom surge, neon glow flare, and bar brightness lift together."
  },
  "visualizer.bloom": {
    "description": "Bloom glow post-processing — bright bars, peak flashes, and the neon line core bleed a soft additive halo. Applies to all modes."
  },
  "visualizer.bloom_intensity": {
    "description": "Bloom glow strength (0.0 disables; up to 1.0 for maximum additive glow)."
  },
  "visualizer.crt": {
    "description": "CRT/film composite post-process — chromatic aberration, scanlines, vignette, grain, and a beat zoom-punch under one master amount (0.0 disables; up to 1.0)."
  },
  "visualizer.height_percent": {
    "description": "Percentage of window height occupied by the visualizer (0.1 to 0.6)."
  },
  "visualizer.higher_cutoff_freq": {
    "description": "Treble ceiling in Hz. Capped at the Nyquist limit (22050)."
  },
  "visualizer.lines.animation_speed": {
    "description": "Color cycle speed for the `breathing` gradient mode (0.05 = very slow, 1.0 = very fast)."
  },
  "visualizer.lines.boat": {
    "description": "Render a small boat sprite that rides the waveform."
  },
  "visualizer.lines.echo": {
    "description": "Milkdrop-style zoom/rotate feedback — the line spirals and tunnels into itself, swirling with the bass/beat (0.0 disables; 1.0 = strong persistence). Takes over the display when on."
  },
  "visualizer.lines.fill_opacity": {
    "description": "Transparency of the area under the line (0.0 to 1.0)."
  },
  "visualizer.lines.glow_intensity": {
    "description": "Neon glow halo around the main line (0.0 disables; up to 1.0). An exponential emissive falloff beyond the stroke that brightens with loudness."
  },
  "visualizer.lines.gradient_mode": {
    "description": "Color mapping mode. [Learn more &rarr;](/reference/visualizer/#gradient-modes-1)"
  },
  "visualizer.lines.line_thickness": {
    "description": "Thickness of the line as a fraction of visualizer height (0.01 to 0.10)."
  },
  "visualizer.lines.mirror": {
    "description": "Symmetric oscilloscope mode (extends from center)."
  },
  "visualizer.lines.outline_opacity": {
    "description": "Outline opacity (0.0 to 1.0)."
  },
  "visualizer.lines.outline_thickness": {
    "description": "Outline border thickness in pixels (0.0 disables; up to 5.0)."
  },
  "visualizer.lines.placement": {
    "description": "Where the Lines visualizer is drawn. Options: `over_cover` (over the now-playing cover art on the Queue view, while playing), `bottom_band` (a band across the bottom of the window, above the player bar, on every view)."
  },
  "visualizer.lines.point_count": {
    "description": "Number of points used to draw the line (8 to 512)."
  },
  "visualizer.lines.style": {
    "description": "Interpolation style (`smooth` or `angular`)."
  },
  "visualizer.lines.trails": {
    "description": "Motion trails — the line leaves a fading after-image (0.0 disables; 1.0 = long comet trails)."
  },
  "visualizer.lower_cutoff_freq": {
    "description": "Bass floor in Hz. Frequencies below this are excluded from the spectrum (minimum `20`)."
  },
  "visualizer.monstercat": {
    "description": "Monstercat-style energy spread to neighbouring bars. Values below `0.7` snap to `0.0` (disabled); effective range is `0.7` to `10.0`."
  },
  "visualizer.noise_reduction": {
    "description": "Smoothing of the raw FFT data (0.0 to 1.0)."
  },
  "visualizer.opacity": {
    "description": "Transparency of the visualizer (0.0 to 1.0)."
  },
  "visualizer.scope.animation_speed": {
    "description": "Color cycle speed for the `breathing` gradient mode (0.05 = very slow, 1.0 = very fast)."
  },
  "visualizer.scope.beam": {
    "description": "Luminous-beam look: additive blending so the glow accumulates into a bright neon beam over the cover (woscope-style)."
  },
  "visualizer.scope.echo": {
    "description": "Milkdrop-style zoom/rotate feedback — the ring spirals and tunnels inward, swirling with the bass/beat (0.0 disables; 1.0 = strong persistence). Takes over the display when on."
  },
  "visualizer.scope.fill_opacity": {
    "description": "Radial gradient fill from the ring toward the center (0.0 = no fill, 1.0 = opaque rim)."
  },
  "visualizer.scope.glow_intensity": {
    "description": "Neon glow halo around the ring (0.0 disables; up to 1.0)."
  },
  "visualizer.scope.gradient_mode": {
    "description": "Color mapping mode. Options: `breathing`, `static`, `position`, `height`, `gradient`."
  },
  "visualizer.scope.line_thickness": {
    "description": "Ring thickness as a fraction of the panel size (0.005 to 0.1)."
  },
  "visualizer.scope.outline_opacity": {
    "description": "Outline opacity (0.0 to 1.0)."
  },
  "visualizer.scope.outline_thickness": {
    "description": "Outline thickness in pixels behind the ring (0.0 disables; up to 5.0)."
  },
  "visualizer.scope.particle_count": {
    "description": "Number of particles in the field (0 disables; up to 2048)."
  },
  "visualizer.scope.particle_speed": {
    "description": "Particle launch-speed multiplier — how fast they fly out from the ring (0.1 = lazy drift, 4.0 = energetic)."
  },
  "visualizer.scope.particles": {
    "description": "Glowing particle field drifting out from the ring (the NCS/Wav2Bar look)."
  },
  "visualizer.scope.point_count": {
    "description": "Points around the ring; more = finer waveform detail (16 to 512)."
  },
  "visualizer.scope.radius": {
    "description": "Mean ring radius as a fraction of the space inside the cover (0.1 = tiny inner ring, 0.95 = nearly fills the panel)."
  },
  "visualizer.scope.sensitivity": {
    "description": "Waveform swing/gain — how far loud audio pushes the ring in and out (0.5 = subtle, 5.0 = wild)."
  },
  "visualizer.scope.style": {
    "description": "Interpolation style around the ring (`smooth` or `angular`)."
  },
  "visualizer.scope.trails": {
    "description": "Motion trails — the ring leaves a fading after-image (0.0 disables; 1.0 = long comet trails)."
  },
  "visualizer.waves": {
    "description": "Enable spline smoothing between bars."
  },
  "visualizer.waves_smoothing": {
    "description": "Spline control-point density when `waves` is on (2 to 16; higher = smoother curves)."
  },
  "volume_normalization_mode": {
    "description": "Volume-normalization mode. Options: `off`, `replay_gain_track`, `replay_gain_album`, `agc`. See the [Audio guide](/guides/audio/#volume-normalization)."
  }
}
