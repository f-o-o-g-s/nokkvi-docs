---
title: Overview
description: A tour of Nokkvi's interface — views, navigation, and the player bar.
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';

export const link = (path) => `${import.meta.env.BASE_URL.replace(/\/$/, '')}/${path.replace(/^\//, '')}`;

Nokkvi is a desktop music client for [Navidrome](https://www.navidrome.org/). It's keyboard-driven and built around a persistent playback queue.

## Interface Layout

Every screen shares the same three-part layout:

- **Nav bar** — switches between views. The [`nav_layout`](/reference/config/#interface-settings) setting controls position: `top` (horizontal tabs, default), `side` (vertical sidebar), or `none` (no nav chrome — only the active page and player bar render).
- **Main content area** — the active view.
- **Player bar** — always visible at the bottom. Shows the current track, progress scrubber, playback controls, and volume.

## Views

Switch views with the number keys or by clicking the nav bar.

| Key | View | What it's for |
| :-- | :--- | :------------ |
| `1` | Queue | Active playback queue — what's playing now and what's up next |
| `2` | Albums | Browse your library by album |
| `3` | Artists | Browse by artist |
| `4` | Songs | Flat list of every track, searchable and sortable |
| `5` | Genres | Browse by genre |
| `6` | Playlists | Navidrome playlists — manual and rule-based smart playlists |
| `7` | Radios | Radio stations |
| `8` | Harbour | Home view — discovery shelves and Nokkvi's whole-library search |
| `` ` `` | Settings | App configuration |

## Expanding Items

Albums, Artists, Genres, and Playlists are single scrollable lists. Press `Shift + Enter` on a focused item to expand it inline — its contents render indented directly below the row (an artist's albums, an album's or playlist's tracks, a genre's albums). Only one item is expanded at a time; press `Shift + Enter` again or `Escape` to collapse it.

While on the Queue view, press `Ctrl + E` to open the **Library Browser** — a side panel for browsing and dragging tracks directly into the queue. See the [Queue guide](/guides/queue/) for details.

## Navigation

Nokkvi is fully keyboard-navigable. The core keys work the same in every view:

| Key | Action |
| :-- | :----- |
| `Tab` / `Backspace` | Move down / up in the list |
| `Enter` | Activate focused item |
| `Shift + Enter` | Expand / collapse the focused item inline |
| `/` | Open search in the current view |
| `Escape` | Close search, collapse the expanded item, or go back |
| `Shift + C` | Scroll the list to the currently playing track |

See the [Keyboard Shortcuts](/reference/hotkeys/) reference for the full list.

## Search

Press `/` in any browsable view to open the inline search bar. Nokkvi filters the list as you type, requesting matching results from the server. Press `Escape` to unfocus the search bar — the filter stays active. Use the clear button inside the search bar to reset it. In the Settings view, `Escape` clears the search and restores the full list.

---

## What's next?

<CardGrid>
  <LinkCard
    title="Queue"
    description="Learn the playback queue, synced lyrics, Library Browser, and drag-to-queue workflow."
    href={link("/guides/queue/")}
  />
  <LinkCard
    title="Harbour"
    description="Nokkvi's home view — discovery shelves, cover previews, and a whole-library search."
    href={link("/guides/harbour/")}
  />
  <LinkCard
    title="Trawl"
    description="Build a mix from a crate of seeds — artists, albums, songs, genres, and playlists."
    href={link("/guides/trawl/")}
  />
  <LinkCard
    title="Smart Playlists"
    description="Rule-based playlists the server keeps up to date — a keyboard-first rules editor with live preview."
    href={link("/guides/smart-playlists/")}
  />
  <LinkCard
    title="Audio"
    description="Configure the equalizer, crossfade, volume normalization, and bit-perfect output."
    href={link("/guides/audio/")}
  />
  <LinkCard
    title="Theming"
    description="Create a custom color theme or tweak one of the 23 built-in themes."
    href={link("/guides/theming/")}
  />
  <LinkCard
    title="Keyboard Shortcuts"
    description="Full reference of every hotkey and how to remap them in config.toml."
    href={link("/reference/hotkeys/")}
  />
  <LinkCard
    title="Configuration Reference"
    description="Every setting in config.toml — defaults, options, and what each one does."
    href={link("/reference/config/")}
  />
  <LinkCard
    title="Visualizer"
    description="Understand gradient modes, peak behavior, and the bars, lines, and scope modes."
    href={link("/reference/visualizer/")}
  />
</CardGrid>
